"use client";
import { useRef,useState } from "react";
export default function Settings(){const input=useRef<HTMLInputElement>(null),[message,setMessage]=useState("");async function backup(){const r=await fetch("/api/backup"),blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`晴天备份-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url);setMessage("备份已下载，请妥善保存")};async function restore(file?:File){if(!file)return;setMessage("正在导入…");try{const data=JSON.parse(await file.text()),r=await fetch("/api/backup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)}),d=await r.json();setMessage(r.ok?`导入完成，共处理 ${d.imported} 条记录`:d.error)}catch{setMessage("文件无法识别，请选择晴天导出的备份")}}
return <main className="app-shell"><header className="app-header"><div className="brand"><span className="brand-sun">☀</span>晴天</div><span>手机与数据</span></header><section className="settings-page"><div className="page-heading"><span>便捷使用</span><h1>把晴天放进口袋</h1><p>无需注册账号，每台设备都有自己的私密记录空间。</p></div><article className="setting-card"><span className="setting-icon">📲</span><div><h2>添加到手机主屏幕</h2><p><b>iPhone：</b>Safari 打开 → 分享 → 添加到主屏幕<br/><b>Android：</b>浏览器菜单 → 添加到主屏幕 / 安装应用</p></div></article><article className="setting-card"><span className="setting-icon">🔒</span><div><h2>记录彼此隔离</h2><p>你和朋友使用同一个链接，但每台设备只会看到自己的记录。无需 GPT 账号。</p></div></article><div className="backup-card"><h2>备份与换机</h2><p>设备身份保存在浏览器中。换手机、清理浏览器前，请先导出备份。</p><div><button onClick={backup}>导出我的数据</button><button className="secondary" onClick={()=>input.current?.click()}>导入备份</button><input ref={input} hidden type="file" accept="application/json" onChange={e=>restore(e.target.files?.[0])}/></div>{message&&<p className="setting-message">{message}</p>}</div><aside className="privacy-note">建议只在自己的手机上使用，不要使用无痕模式。备份文件包含你的心情随笔，请勿随意转发。</aside></section></main>}
