import { eq } from "drizzle-orm";import { getDb } from "../../../db";import { journalNotes,moodEntries } from "../../../db/schema";import { getDeviceId } from "../../device";
export async function GET(){const ownerId=await getDeviceId(),db=getDb();const [entries,notes]=await Promise.all([db.select().from(moodEntries).where(eq(moodEntries.ownerId,ownerId)),db.select().from(journalNotes).where(eq(journalNotes.ownerId,ownerId))]);return Response.json({app:"晴天",version:1,exportedAt:new Date().toISOString(),entries:entries.map(({id:_,ownerId:__,...e})=>e),notes:notes.map(({id:_,ownerId:__,...n})=>n)},{headers:{"content-disposition":`attachment; filename="qingtian-backup.json"`}})}
export async function POST(request:Request){try{const ownerId=await getDeviceId(),data=await request.json() as {entries?:Array<Record<string,unknown>>;notes?:Array<Record<string,unknown>>};if(!Array.isArray(data.entries)||!Array.isArray(data.notes))return Response.json({error:"备份文件格式不正确"},{status:400});const db=getDb();let imported=0;for(const raw of data.entries.slice(0,1000)){const date=String(raw.date??"");if(!/^\d{4}-\d{2}-\d{2}$/.test(date))continue;const values={ownerId,date,mood:String(raw.mood??"calm").slice(0,40),moodLabel:String(raw.moodLabel??"平静").slice(0,20),moodIcon:String(raw.moodIcon??"😌").slice(0,8),summaryManual:Boolean(raw.summaryManual),note:"",updatedAt:String(raw.updatedAt??new Date().toISOString())};await db.insert(moodEntries).values(values).onConflictDoUpdate({target:[moodEntries.ownerId,moodEntries.date],set:values});imported++}for(const raw of data.notes.slice(0,5000)){const date=String(raw.date??""),content=String(raw.content??"").trim().slice(0,500);if(!/^\d{4}-\d{2}-\d{2}$/.test(date)||!content)continue;await db.insert(journalNotes).values({ownerId,date,content,mood:String(raw.mood??"calm").slice(0,40),moodLabel:String(raw.moodLabel??"平静").slice(0,20),moodIcon:String(raw.moodIcon??"😌").slice(0,8),moodScore:Math.max(1,Math.min(5,Number(raw.moodScore)||3)),recordedAt:String(raw.recordedAt??new Date().toISOString()),updatedAt:String(raw.updatedAt??new Date().toISOString())});imported++}return Response.json({imported})}catch{return Response.json({error:"导入失败，请确认文件来自晴天"},{status:400})}}
